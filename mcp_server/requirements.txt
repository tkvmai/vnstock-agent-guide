# vnstock MCP Server dependencies
# Install into your vnstock virtual environment:
#   pip install -r requirements.txt

fastmcp>=0.4.0
pandas>=2.0.0
numpy>=1.24.0
tabulate>=0.9.0      # Required by df.to_markdown()

# vnstock ecosystem (install separately per your license tier)
# Free tier:
#   pip install -U vnstock
#
# Sponsored tier (Bronze/Silver/Golden/Diamond):
#   pip install vnstock_data vnstock_ta vnstock_news
#
# Golden/Diamond (pipeline & streaming):
#   pip install vnstock_pipeline
